%header
  #info
    %h1
      %span.pictos m
      Warble
    #profile
      %span#toolbar
        %a#settings(href='#' title='Settings') y
        = link_to '-', destroy_user_session_path, title: 'Logout', method: 'delete'

  #playing

  #controls
    %span.vote
    %a#forward(href='#') 7
    #volume(data-volume='#{@volume}')

#add
#spinner
  = image_tag 'spinner.png'
#queue
%ul#songs

- content_for :scripts do
  = javascript_include_tag 'application', debug: Rails.env.development?
