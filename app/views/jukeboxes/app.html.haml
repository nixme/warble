%header
  #info
    #profile
      %span#toolbar
        %a#settings(href='#' title='Settings') y
        = link_to '-', logout_path, title: 'Logout', method: 'delete'

#left
  %aside#playing

  %aside#controls
    %span.vote
    %a#forward(href='#') 7
    #volume(data-volume='#{@volume}')

  %ul#songs

#add
  .tabs
  .content

#spinner
  = image_tag 'spinner.png'
#queue

- content_for :scripts do
  = javascript_include_tag 'application', debug: Rails.env.development?
